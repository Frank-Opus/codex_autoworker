---
name: autoworker-status
description: "Inspect the durable Autoworker workflow snapshot for the current repo."
---

Run `python3 scripts/autoworker_state.py --write-state` from the repository root and use its output as the canonical workflow snapshot for resume guidance.
