# Legacy Skill Source

This directory is kept temporarily as migration source material while the Codex-native port stabilizes.

- Codex should use `.agents/skills/` as the canonical runtime skill tree.
- Files here may lag behind the Codex-facing copies during the migration milestone.
- Remove or archive this directory once the migration milestone is fully closed.
