# Legacy Skill Source

This directory is not the primary Codex runtime surface.

- Use `.agents/skills/` as the canonical workflow tree.
- Treat files here as migration-era source material or compatibility residue.
- Expect the contents here to lag behind the Codex-facing copies.
- If behavior changes, update `.agents/skills/` first and only sync here if that legacy copy still matters.
